(() => {
  'use strict';
  if (!window.MZV) return;

  const BUILD = '4.6.9.7-local-leaderboard';
  const PROFILE_KEY = 'mzv.localPlayer.v1';
  const BOARD_KEY = 'mzv.localLeaderboard.v1';
  const MAX_ENTRIES = 30;
  const FINAL_LEVEL = 50;
  const SNAPSHOT_SIZE = 20;
  const SEED_NAMES = ['NIGHTRUNNER','MAPUTO_77','ZEDHUNTER','NEONWOLF','DARKMAMBA','GHOSTUNIT','KATEMBE','VAMPKILLER','REDZONE','LASTSTAND','MZ_SURVIVOR','BLACKOUT','RAGEKING','MOONWALKER','DEADEND','HAVOC_88','LONEWOLF','THRILLRIDE','NIGHTSHIFT','GRAVEYARD'];
  const safeJson = (raw, fallback) => { try { return raw ? JSON.parse(raw) : fallback; } catch { return fallback; } };
  const uid = () => { try { if (crypto?.randomUUID) return crypto.randomUUID(); } catch {} return `local-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 10)}`; };
  const sanitizeName = value => String(value || '').replace(/[^\p{L}\p{N}_\- ]/gu, '').replace(/\s+/g, ' ').trim().slice(0, 14).toUpperCase();
  const difficultyId = () => window.MZV_SELECTED_DIFFICULTY || 'easy';
  const boardKey = s => `${s.mission || 'deserto'}|${difficultyId()}|${s.mode || 'single'}`;
  const labelMission = s => s.mission === 'thriller' ? 'THRILLER' : 'DESERTO';
  const labelMode = s => s.mode === 'two' ? 'CO-OP' : 'SOLO';
  const labelDifficulty = () => difficultyId().toUpperCase();
  const fmt = n => Math.max(0, Math.round(n || 0)).toLocaleString('pt-PT');
  const ordinal = n => `${n}.º`;

  class LocalLeaderboardStore {
    loadProfile() { const p = safeJson(localStorage.getItem(PROFILE_KEY), null); return p && p.id && p.name ? p : null; }
    saveProfile(name) { const existing=this.loadProfile(); const profile={id:existing?.id||uid(),name:sanitizeName(name),createdAt:existing?.createdAt||Date.now()}; localStorage.setItem(PROFILE_KEY,JSON.stringify(profile)); return profile; }
    loadBoards() { return safeJson(localStorage.getItem(BOARD_KEY), {}); }
    saveBoards(boards) { localStorage.setItem(BOARD_KEY, JSON.stringify(boards)); }
    seedFor(key) {
      const [mission,difficulty,mode]=key.split('|');
      const diffFactor={easy:1,hard:1.45,nightmare:2.15,thriller:3.05}[difficulty]||1;
      const missionFactor=mission==='thriller'?1.18:1, modeFactor=mode==='two'?1.22:1, top=158000*diffFactor*missionFactor*modeFactor;
      return SEED_NAMES.map((name,i)=>{const decay=Math.pow(.925,i),texture=1+(((i*37)%11)-5)/160;return{playerId:`seed-${i+1}`,name,score:Math.round(top*decay*texture),kills:Math.round(650*decay+85),level:Math.max(8,Math.round(FINAL_LEVEL*(.96-i*.018))),duration:Math.round(780+i*32),seeded:true,updatedAt:0};}).sort((a,b)=>b.score-a.score);
    }
    ensureBoard(key) { const boards=this.loadBoards(); if(!Array.isArray(boards[key])||!boards[key].length){boards[key]=this.seedFor(key);this.saveBoards(boards);} return boards[key]; }
    snapshot(key,playerId) { const all=this.ensureBoard(key).slice().sort((a,b)=>b.score-a.score); const own=all.find(x=>x.playerId===playerId)||null; return{all,own,competitors:all.filter(x=>x.playerId!==playerId).slice(0,SNAPSHOT_SIZE)}; }
    submit(key,run) { const boards=this.loadBoards(),list=Array.isArray(boards[key])?boards[key].slice():this.seedFor(key),idx=list.findIndex(x=>x.playerId===run.playerId),prev=idx>=0?list[idx]:null,isPersonalBest=!prev||run.score>prev.score; if(isPersonalBest){if(idx>=0)list[idx]=run;else list.push(run);} list.sort((a,b)=>b.score-a.score);boards[key]=list.slice(0,MAX_ENTRIES);this.saveBoards(boards);const official=boards[key],finalEntry=official.find(x=>x.playerId===run.playerId)||prev||run,rank=Math.max(1,official.findIndex(x=>x.playerId===finalEntry.playerId)+1);return{rank,isPersonalBest,entry:finalEntry,board:official}; }
  }
  const store=new LocalLeaderboardStore();

  function ensureStyles(){
    if(document.getElementById('localLeaderboardStyles'))return;
    const style=document.createElement('style');style.id='localLeaderboardStyles';style.textContent=`
#localNameOverlay{position:fixed;inset:0;z-index:120;display:flex;align-items:center;justify-content:center;background:rgba(3,7,12,.88);backdrop-filter:blur(7px)}#localNameOverlay.hidden{display:none!important}.local-name-card{width:min(520px,92vw);padding:28px;border-radius:22px;background:linear-gradient(180deg,rgba(18,28,39,.98),rgba(8,14,22,.98));border:1px solid rgba(73,198,255,.55);box-shadow:0 0 38px rgba(46,200,255,.16),0 30px 90px rgba(0,0,0,.5)}.local-name-card h2{margin:0 0 8px;font-size:30px}.local-name-card p{color:#aeb9c7;line-height:1.45}.local-name-card input{width:100%;font-size:24px;font-weight:900;letter-spacing:.08em;text-transform:uppercase;padding:14px 16px;border-radius:14px;border:2px solid #2ec8ff;background:#08111c;color:#ffe456;outline:none}.local-name-card .name-error{height:22px;color:#ff7e87;font-weight:800;margin-top:8px}.local-name-actions{display:flex;justify-content:flex-end;margin-top:12px}.local-name-actions button{border-color:#ffe456;background:#ffe456;color:#111827;padding:11px 18px;border-radius:12px}#localPlayerBadge{margin-top:14px;display:inline-flex;gap:9px;align-items:center;padding:7px 11px;border:1px solid #2ec8ff;border-radius:999px;background:rgba(8,17,28,.72);color:#8deaff;font-size:12px;font-weight:900;cursor:pointer}#localPlayerBadge b{color:#ffe456}
#localRankPulse{position:absolute;z-index:32;left:50%;top:15%;transform:translate(-50%,-10px);opacity:0;pointer-events:none;padding:9px 14px;border-radius:12px;background:rgba(5,12,21,.72);border:1px solid rgba(46,200,255,.7);color:#fff;font-weight:950;font-size:14px;text-align:center;box-shadow:0 0 24px rgba(46,200,255,.18);transition:.18s;max-width:min(620px,86vw)}#localRankPulse.show{opacity:1;transform:translate(-50%,0)}#localRankPulse strong{color:#ffe456}#localLeaderboardPeek{position:absolute;z-index:31;width:min(300px,38vw);min-width:230px;pointer-events:none;padding:12px 13px;border-radius:16px;background:linear-gradient(135deg,rgba(5,12,22,.70),rgba(8,18,31,.54));border:1px solid rgba(46,200,255,.55);box-shadow:0 0 28px rgba(46,200,255,.12);backdrop-filter:blur(3px);opacity:0;transition:.22s}#localLeaderboardPeek.show{opacity:1}.lb-tl{left:14px;top:14px}.lb-tr{right:14px;top:14px}.lb-bl{left:14px;bottom:14px}.lb-br{right:14px;bottom:14px}.lb-head{display:flex;justify-content:space-between;gap:8px;align-items:center;font-size:11px;font-weight:950;color:#8deaff;margin-bottom:7px}.lb-head b{color:#ffe456}.lb-row{display:grid;grid-template-columns:28px 1fr auto;gap:7px;align-items:center;padding:3px 0;font-size:12px;font-weight:850;border-bottom:1px solid rgba(255,255,255,.055)}.lb-row.you{color:#ffe456}.lb-rank{color:#72dfff}.lb-foot{margin-top:7px;color:#b8c7d4;font-size:10px;font-weight:800;line-height:1.35}.lb-foot b{color:#ffe456}#localFinalBoard{margin-top:15px;padding:13px;border-radius:14px;background:rgba(8,17,28,.75);border:1px solid rgba(46,200,255,.35);text-align:left}.local-final-title{font-size:13px;font-weight:950;color:#8deaff;margin-bottom:8px}.local-final-row{display:grid;grid-template-columns:34px 1fr auto;gap:8px;padding:3px 0;font-size:12px;font-weight:800}.local-final-row.you{color:#ffe456}.local-final-meta{margin-top:9px;color:#aeb9c7;font-size:11px;line-height:1.4}@media(max-width:760px){#localLeaderboardPeek{width:224px;min-width:0;padding:9px 10px}.lb-row{font-size:10px;grid-template-columns:24px 1fr auto;gap:4px}.lb-head,.lb-foot{font-size:9px}#localRankPulse{top:12%;font-size:11px;padding:7px 10px}.local-name-card{padding:20px}.local-name-card h2{font-size:24px}.local-name-card input{font-size:19px}}`;
    document.head.appendChild(style);
  }

  function ensureUi(){
    ensureStyles();const gameWrap=document.getElementById('gameWrap');
    if(gameWrap&&!document.getElementById('localRankPulse')){const pulse=document.createElement('div');pulse.id='localRankPulse';gameWrap.appendChild(pulse);const peek=document.createElement('div');peek.id='localLeaderboardPeek';gameWrap.appendChild(peek);}
    if(!document.getElementById('localNameOverlay')){const overlay=document.createElement('div');overlay.id='localNameOverlay';overlay.className='hidden';overlay.innerHTML=`<div class="local-name-card"><h2>NOME DO SOBREVIVENTE</h2><p>Este nome será usado no leaderboard local desta versão. Mais tarde ligaremos o mesmo sistema ao Firebase.</p><input id="localNameInput" maxlength="14" autocomplete="nickname" placeholder="EX.: NIGHTRUNNER"><div id="localNameError" class="name-error"></div><div class="local-name-actions"><button id="localNameSave">ENTRAR NA BATALHA</button></div></div>`;document.body.appendChild(overlay);const save=()=>{const name=sanitizeName(document.getElementById('localNameInput')?.value),err=document.getElementById('localNameError');if(name.length<3){if(err)err.textContent='Usa pelo menos 3 caracteres.';return;}store.saveProfile(name);overlay.classList.add('hidden');updatePlayerBadge();const pending=window.__MZV_PENDING_LOCAL_START__;window.__MZV_PENDING_LOCAL_START__=null;if(pending)pending.app.start(...pending.args);};overlay.querySelector('#localNameSave').addEventListener('click',save);overlay.querySelector('#localNameInput').addEventListener('keydown',e=>{if(e.key==='Enter')save();});}
    const modeMenu=document.getElementById('modeMenu');if(modeMenu&&!document.getElementById('localPlayerBadge')){const badge=document.createElement('button');badge.id='localPlayerBadge';badge.type='button';badge.addEventListener('click',()=>showNameModal(true));modeMenu.appendChild(badge);}
    const hud=document.getElementById('hud');if(hud&&!document.getElementById('localHudPlayer')){const span=document.createElement('span');span.className='pill';span.id='localHudPlayer';span.innerHTML=`🏆 <b id="localHudPlayerName">LOCAL</b>`;hud.appendChild(span);}
    const resultText=document.getElementById('resultText');if(resultText&&!document.getElementById('localFinalBoard')){const board=document.createElement('div');board.id='localFinalBoard';board.className='hidden';resultText.insertAdjacentElement('afterend',board);}updatePlayerBadge();
  }
  function updatePlayerBadge(){const p=store.loadProfile(),badge=document.getElementById('localPlayerBadge');if(badge)badge.innerHTML=p?`🏆 PLAYER <b>${p.name}</b> · ALTERAR`:'🏆 DEFINIR NOME DO SOBREVIVENTE';const hudName=document.getElementById('localHudPlayerName');if(hudName)hudName.textContent=p?.name||'LOCAL';}
  function showNameModal(force=false){ensureUi();const profile=store.loadProfile();if(profile&&!force)return false;const overlay=document.getElementById('localNameOverlay'),input=document.getElementById('localNameInput'),err=document.getElementById('localNameError');if(err)err.textContent='';if(input)input.value=profile?.name||'';overlay?.classList.remove('hidden');setTimeout(()=>input?.focus(),30);return true;}
  function rankFor(score,snapshot){return Math.min(snapshot.length+1,snapshot.filter(x=>x.score>score).length+1);}
  function nextTarget(score,snapshot){const sorted=snapshot.slice().sort((a,b)=>b.score-a.score),position=rankFor(score,sorted);if(position<=1)return null;const target=sorted[position-2];return target?{rank:position-1,score:target.score,diff:Math.max(0,target.score-score)}:null;}
  function projection(s){const snapshot=s.localLbSnapshot||[];if(!snapshot.length)return{score:s.score,rank:1,killsPerMin:0,pointsPerMin:0};const minutes=Math.max(.35,s.elapsed/60),killsPerMin=s.kills/minutes,pointsPerMin=s.score/minutes;if(s.elapsed<55||s.level<=1)return{score:s.score,rank:rankFor(s.score,snapshot),killsPerMin,pointsPerMin};const levelsPerMin=Math.max(.42,(s.level-1)/minutes),remainingLevels=Math.max(0,FINAL_LEVEL-s.level),remainingMinutes=Math.min(30,remainingLevels/levelsPerMin),projectedScore=s.score+pointsPerMin*remainingMinutes*.88;return{score:Math.round(projectedScore),rank:rankFor(projectedScore,snapshot),killsPerMin,pointsPerMin};}
  function showRankPulse(html,ms=2400){const el=document.getElementById('localRankPulse');if(!el)return;el.innerHTML=html;el.classList.add('show');clearTimeout(el.__hideTimer);el.__hideTimer=setTimeout(()=>el.classList.remove('show'),ms);}
  function chooseCorner(app){const s=app.state,r=app.renderer,w=r.canvas.clientWidth,h=r.canvas.clientHeight,zoom=r.zoom||1,cam=s.camera||{x:0,y:0},q={tl:1.4,tr:1.2,bl:1,br:1};const add=(entity,weight)=>{if(!entity||entity.alive===false||entity.out)return;const x=(entity.x-cam.x)*zoom,y=(entity.y-cam.y)*zoom;if(x< -50||x>w+50||y< -50||y>h+50)return;const key=`${y<h/2?'t':'b'}${x<w/2?'l':'r'}`;q[key]+=weight;};for(const p of s.players||[])add(p,8);for(const z of s.enemies||[])add(z,z.isBoss?7:z.isMiniBoss?4:.8);add(s.reinforcement,5);add(s.rescueNpc?.active?s.rescueNpc:null,6);for(const ch of s.chests||[])add(ch,1.5);if(s.elapsed<(s.rageUntil||0))q.tr+=9;if(!r.mobileLayout())q.br+=8;if(r.mobileLayout()){q.bl+=11;q.br+=11;}if(s.carnageActive){q.tl+=2;q.tr+=2;}return Object.entries(q).sort((a,b)=>a[1]-b[1])[0][0];}
  function virtualBoard(s){const profile=store.loadProfile(),list=(s.localLbSnapshot||[]).map(x=>({...x}));list.push({playerId:profile?.id||'you',name:profile?.name||'YOU',score:s.score,current:true});list.sort((a,b)=>b.score-a.score);return list;}
  function renderPeek(app){const s=app.state,peek=document.getElementById('localLeaderboardPeek');if(!peek)return;const list=virtualBoard(s),youIdx=list.findIndex(x=>x.current),top=list.slice(0,5),rows=youIdx>=5?top.slice(0,4).concat(list[youIdx]):top,proj=projection(s),corner=chooseCorner(app);peek.className=`lb-${corner} show`;peek.innerHTML=`<div class="lb-head"><span>LOCAL LEADERBOARD</span><b>${labelMission(s)} · ${labelDifficulty()} · ${labelMode(s)}</b></div>${rows.map(x=>{const rank=list.indexOf(x)+1;return `<div class="lb-row ${x.current?'you':''}"><span class="lb-rank">#${rank}</span><span>${x.current?'YOU · ':''}${x.name}</span><span>${fmt(x.score)}</span></div>`;}).join('')}<div class="lb-foot"><b>${Math.round(proj.killsPerMin)} kills/min</b> · ritmo actual: projecção <b>#${proj.rank}</b><br>Snapshot carregado no início · sem leituras online.</div>`;clearTimeout(peek.__hideTimer);peek.__hideTimer=setTimeout(()=>peek.classList.remove('show'),3000);}

  function initRun(app){ensureUi();const s=app.state,profile=store.loadProfile();if(!profile)return;const key=boardKey(s),snap=store.snapshot(key,profile.id);s.localLbKey=key;s.localLbSnapshot=snap.competitors;s.localLbOwnBest=snap.own;s.localLbLastRank=rankFor(0,snap.competitors);s.localLbNextMessageAt=28;s.localLbNextPeekAt=180;s.localLbNextProjectionAt=95;s.localLbSaved=false;s.localLbRunStartedAt=Date.now();if(nextTarget(0,snap.competitors))setTimeout(()=>{if(app.state===s&&s.running)showRankPulse(`🏆 RANKING LOCAL CARREGADO · <strong>#${s.localLbLastRank}</strong> É A POSIÇÃO DE PARTIDA`,2100);},6500);}
  function updateRun(app){const s=app.state;if(!s.running||!Array.isArray(s.localLbSnapshot))return;const currentRank=rankFor(s.score,s.localLbSnapshot);if(currentRank<(s.localLbLastRank||999)){const target=nextTarget(s.score,s.localLbSnapshot);if(currentRank===1)showRankPulse('⚡ SUBISTE PARA <strong>#1</strong> · ESTÁS A LIDERAR A PROJECÇÃO LOCAL');else if(target)showRankPulse(`⚡ SUBISTE PARA <strong>#${currentRank}</strong> · ${fmt(target.diff)} PTS PARA #${target.rank}`);s.localLbLastRank=currentRank;s.localLbNextMessageAt=s.elapsed+35;}if(s.elapsed>=(s.localLbNextMessageAt||0)){const target=nextTarget(s.score,s.localLbSnapshot);if(!target)showRankPulse('🏆 PROJECÇÃO ACTUAL: <strong>#1</strong> · DEFENDE A LIDERANÇA');else showRankPulse(`🏆 ESTÁS A <strong>${fmt(target.diff)} PONTOS</strong> DO ${ordinal(target.rank)} LUGAR`);s.localLbNextMessageAt=s.elapsed+45;}if(s.elapsed>=(s.localLbNextProjectionAt||0)){const p=projection(s);showRankPulse(`📈 ${Math.round(p.killsPerMin)} KILLS/MIN · AO RITMO ACTUAL: <strong>PROJECÇÃO #${p.rank}</strong>`,2600);s.localLbNextProjectionAt=s.elapsed+120;}if(s.elapsed>=(s.localLbNextPeekAt||0)){renderPeek(app);s.localLbNextPeekAt+=180;}}
  function finalizeRun(app,victory){const s=app.state;if(s.localLbSaved)return s.localLbFinal||null;const profile=store.loadProfile();if(!profile||!s.localLbKey)return null;const run={playerId:profile.id,name:profile.name,score:Math.round(s.score||0),kills:Math.round(s.kills||0),level:Math.round(s.level||1),duration:Math.round(s.elapsed||0),victory:!!victory,difficulty:difficultyId(),mission:s.mission,mode:s.mode,updatedAt:Date.now()};const result=store.submit(s.localLbKey,run);s.localLbSaved=true;s.localLbFinal=result;return result;}
  function renderFinal(app,result){ensureUi();const s=app.state,box=document.getElementById('localFinalBoard');if(!box||!result)return;const profile=store.loadProfile(),board=result.board.slice(0,10),officialScore=result.entry?.score??s.score;box.classList.remove('hidden');box.innerHTML=`<div class="local-final-title">🏆 LOCAL LEADERBOARD · ${labelMission(s)} / ${labelDifficulty()} / ${labelMode(s)}</div>${board.map((x,i)=>`<div class="local-final-row ${x.playerId===profile?.id?'you':''}"><span>#${i+1}</span><span>${x.name}</span><span>${fmt(x.score)}</span></div>`).join('')}<div class="local-final-meta">${result.isPersonalBest?'<b>NOVO RECORDE PESSOAL</b> · ':'Melhor resultado mantido · '}posição local: <b>#${result.rank}</b> · ${fmt(officialScore)} pts.<br>Esta versão usa apenas <b>localStorage</b>. Quando ligarmos Firebase, substituímos só a camada de dados.</div>`;}

  const baseStart=MZV.GameApp.prototype.start;
  MZV.GameApp.prototype.start=function(...args){ensureUi();if(!location.search.includes('autotest=1')&&!store.loadProfile()){window.__MZV_PENDING_LOCAL_START__={app:this,args};showNameModal(false);return;}if(location.search.includes('autotest=1')&&!store.loadProfile())store.saveProfile('AUTOTEST');const out=baseStart.apply(this,args);initRun(this);return out;};
  const baseUpdate=MZV.GameApp.prototype.update;MZV.GameApp.prototype.update=function(dt){const out=baseUpdate.call(this,dt);updateRun(this);return out;};
  const baseShowGameOver=MZV.GameApp.prototype.showGameOver;MZV.GameApp.prototype.showGameOver=function(victory){const result=finalizeRun(this,victory),out=baseShowGameOver.call(this,victory);renderFinal(this,result);return out;};
  if(document.readyState==='loading')window.addEventListener('DOMContentLoaded',()=>ensureUi(),{once:true});else ensureUi();
  window.addEventListener('DOMContentLoaded',()=>{ensureUi();if(!location.search.includes('autotest=1')&&!store.loadProfile())showNameModal(false);document.title='MENESES: ZOMBIES & VAMPIROS — V4.6.9.7 LEADERBOARD';const footer=document.querySelector('footer');if(footer)footer.textContent='V4.6.9.7 · Local Leaderboard & Rank Projection · Rage Neon · Boss Hints · Thriller Tension';const copy=document.querySelector('.version-copy');if(copy)copy.innerHTML='V4.6.9.7 · <b>LEADERBOARD</b> · Nome do jogador, ranking, projecção e leaderboard contextual.';},{once:true});
  console.info('[MZV]',BUILD,'loaded');
})();
